<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal baseado em Material Components -->
    <style name="Theme.SasakiFlix" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/netflix_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/netflix_red</item>
    </style>

</resources>
